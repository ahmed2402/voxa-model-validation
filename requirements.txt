# STT — direct GPU inference via openai-whisper (NOT whisper-live server)
openai-whisper

# Audio loading — numpy<2.0 required; whisper's librosa breaks on numpy 2.x
numpy<2.0

# LLM client — talks to vLLM's OpenAI-compatible server
openai>=1.0.0

# TTS
voxcpm
soundfile

# Utilities
pyyaml>=6.0
rich>=13.0.0
httpx>=0.24.0
